{
  "fmt": {
    "options": {
      "lineWidth": 200,
      "semiColons": false
    }
  },
  "tasks": {
    "update-deps": "deno run --allow-read=. --allow-write=. --allow-net https://deno.land/x/udd/main.ts --dry-run deps.ts",
    "build-schema": "deno run --allow-write tools/build-schema.ts",
    "build": "deno task build-schema"
  }
}
