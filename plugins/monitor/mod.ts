/**
 * Main entrypoint of the Pup plugin 'monitor'
 *
 * @file plugins/monitor/mod.ts
 */

/*
import { ApplicationState } from "../../lib/core/status.ts"
import { PluginApi, PluginConfiguration, PluginImplementation } from "../../mod.ts"

export class SplunkReporterPlugin extends PluginImplementation {
  constructor(private pup: PluginApi, private config: PluginConfiguration) {
    super(pup, config)
    this.meta = {
      name: "monitor",
      version: "1.0.0",
      api: "1",
      repository: "https://github.com/hexagon/pup",
    }

    this.setupEventListeners()
  }

  private setupEventListeners() {
    this.pup.events.on("application_state", async (eventData?: ApplicationState) => {
      if (eventData) {

      }
    })
  }
}
*/
