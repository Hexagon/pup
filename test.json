{
  "processes": [
    {
      "name": "kept-alive-server",
      "cmd": ["deno", "run", "--allow-read", "./examples/basic/server.js"],
      "autostart": true,
      "restart": "always",
      "restartDelayMs": 10000
    },
    {
      "name": "periodic-example-task",
      "cmd": ["deno", "run", "--allow-read", "./examples/basic/task.js"],
      "startPattern": "*/5 * * * * *"
    }
  ]
}
